export function checkIfUsernameExists(username: string): boolean {
    //check if username exists in db
    return false;
}

export function checkIfEmailExists(email: string): boolean {
    //check if email exists in db
    return false;
}